---
phase: 01-port-map-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Map.tsx
autonomous: true
requirements:
  - MAP-ATTR
must_haves:
  truths:
    - "Default full-text attribution bar is no longer visible on the map"
    - "A small (i) icon is visible in the bottom-right corner"
    - "Clicking the (i) icon expands to show full Mapbox attribution text"
  artifacts:
    - path: "src/pages/Map.tsx"
      provides: "Compact attribution control"
      contains: "attributionControl: false"
  key_links:
    - from: "Map constructor options"
      to: "mapboxgl.AttributionControl"
      via: "addControl after map construction"
      pattern: "AttributionControl.*compact.*true"
---

<objective>
Replace the default full-text Mapbox attribution bar with the compact (i) icon.

Purpose: Remove visual clutter from the map while remaining compliant with Mapbox ToS (attribution is still accessible via click).
Output: Map.tsx updated — attribution shows only the (i) icon, expands on click.
</objective>

<execution_context>
@S:/Claude/routed/.planning/phases/01-port-map-enhancements-3d-terrain-tilt-topo-satellite-toggle-/01-RESEARCH.md
</execution_context>

<context>
@S:/Claude/routed/.planning/ROADMAP.md
@S:/Claude/routed/src/pages/Map.tsx

<interfaces>
<!-- From src/pages/Map.tsx — the map initialization block (lines 285-292): -->

```typescript
const map = new mapboxgl.Map({
  container: mapContainerRef.current,
  style: MAP_STYLE_TERRAIN,
  center: [133.7751, -25.2744],
  zoom: 4,
})

map.addControl(new mapboxgl.NavigationControl(), 'top-right')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compact attribution control to map initialization</name>
  <files>src/pages/Map.tsx</files>
  <action>
In the map initialization useEffect (around line 285), make two changes:

1. Add `attributionControl: false` to the Map constructor options to disable the default full-text attribution bar:

```typescript
const map = new mapboxgl.Map({
  container: mapContainerRef.current,
  style: MAP_STYLE_TERRAIN,
  center: [133.7751, -25.2744],
  zoom: 4,
  attributionControl: false,
})
```

2. Immediately after the existing `map.addControl(new mapboxgl.NavigationControl(), 'top-right')` line, add the compact attribution control:

```typescript
map.addControl(new mapboxgl.AttributionControl({ compact: true }))
```

This places the compact (i) icon at the default `bottom-right` position. The NavigationControl remains at `top-right` — no conflict.

Do NOT remove or modify the NavigationControl line. Do NOT change any other part of the map init.
  </action>
  <verify>
    <automated>cd S:/Claude/routed && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Map constructor has `attributionControl: false`
    - `new mapboxgl.AttributionControl({ compact: true })` is added via `addControl`
    - TypeScript compiles without errors
    - No other map init code is changed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Attribution is a UI-only change using the built-in Mapbox control.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | R (Repudiation) | Attribution removal | accept | compact: true still shows attribution on click — Mapbox ToS satisfied |
</threat_model>

<verification>
Run `npm run dev` and open the Map tab:
1. Confirm no full-text attribution bar visible at the bottom of the map
2. Confirm a small (i) icon is visible in the bottom-right corner
3. Click the (i) icon — full attribution text should expand
4. Confirm NavigationControl (zoom +/-) still renders at top-right
</verification>

<success_criteria>
- Full-text attribution bar is gone
- Compact (i) icon present in bottom-right
- Click expands to show full attribution
- No regressions to map functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-port-map-enhancements-3d-terrain-tilt-topo-satellite-toggle-/01-01-SUMMARY.md`
</output>
