---
phase: 03-destination-photos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/destinations.ts
autonomous: true
requirements:
  - PHOTO-DATA

must_haves:
  truths:
    - "Every destination in the array has a photos field containing at least one URL"
    - "The Destination interface includes photos: string[]"
    - "All 70 photo URLs point to images.unsplash.com with w=800 and auto=format parameters"
  artifacts:
    - path: "src/data/destinations.ts"
      provides: "Destination interface with photos field + all 70 entries populated"
      contains: "photos: string[]"
  key_links:
    - from: "src/data/destinations.ts"
      to: "Unsplash CDN"
      via: "static URL strings"
      pattern: "https://images\\.unsplash\\.com/photo-"
---

<objective>
Add the `photos: string[]` field to the Destination interface and populate every one of the 70 destination entries with a curated Unsplash hero image URL.

Purpose: Provides the data layer that the popup and card display tasks (Plan 02) depend on. Without photos data, there is nothing to render.
Output: Updated `src/data/destinations.ts` with `photos` field on the interface and a `photos: ["https://images.unsplash.com/photo-...?w=800&auto=format"]` array on every entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-destination-photos/03-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/data/destinations.ts:
```typescript
export interface Destination {
  id: string;
  name: string;
  description: string;
  lat: number;
  lng: number;
  nearestTown: string;
  region: string;
  campsites: string[];
  campsiteCostPerNight: number;
  activities: Activity[];
  roadType: RoadType;
  bestSeasons: Season[];
  tripLength: TripLength[];
  crewNotes: string | null;
  fishSpecies: string[];
  tags: string[];
  bookingInfo: BookingInfo;
}
```

The file has 70 destination entries and is 1619 lines long. Each entry follows the same object shape. The `photos` field must be added to the interface AND to every entry object.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add photos field to Destination interface</name>
  <files>src/data/destinations.ts</files>
  <read_first>src/data/destinations.ts (lines 1-47 -- the interface definition)</read_first>
  <action>
Add `photos: string[];` as the last field in the `Destination` interface, after the `bookingInfo` field (per D-01). The field is non-optional so TypeScript will enforce that every entry has it populated.

The interface should become:
```typescript
export interface Destination {
  id: string;
  name: string;
  description: string;
  lat: number;
  lng: number;
  nearestTown: string;
  region: string;
  campsites: string[];
  campsiteCostPerNight: number;
  activities: Activity[];
  roadType: RoadType;
  bestSeasons: Season[];
  tripLength: TripLength[];
  crewNotes: string | null;
  fishSpecies: string[];
  tags: string[];
  bookingInfo: BookingInfo;
  photos: string[];
}
```
  </action>
  <acceptance_criteria>
    - `src/data/destinations.ts` contains `photos: string[];` inside the `Destination` interface
    - The field is non-optional (no `?` before the colon)
    - The field appears after `bookingInfo: BookingInfo;`
  </acceptance_criteria>
  <verify>
    <automated>grep -n "photos: string\[\]" src/data/destinations.ts | head -1</automated>
  </verify>
  <done>Destination interface includes `photos: string[]` as a required field</done>
</task>

<task type="auto">
  <name>Task 2: Curate and add Unsplash photo URLs for all 70 destinations</name>
  <files>src/data/destinations.ts</files>
  <read_first>src/data/destinations.ts (full file -- need every destination's id and name to curate appropriate photos)</read_first>
  <action>
Add a `photos` array to each of the 70 destination entries in the `destinations` array (per D-02, D-03, D-04). Each entry gets exactly one Unsplash CDN URL as the first (and only) element.

**URL format (per D-03):** `https://images.unsplash.com/photo-{ID}?w=800&auto=format`

**Curation rules:**
- Search Unsplash for landscape/nature photos matching each destination name, region, or nearest town
- Prioritise: scenic landscape shots, water features (rivers/lakes for fishing destinations), mountain/bush scenes for High Country destinations, coastal shots for Gippsland coast/Great Ocean Road destinations
- Every destination MUST have a URL -- no empty arrays, no placeholders
- The `w=800` query parameter controls rendered width (per Claude's Discretion in CONTEXT.md)
- Include `&auto=format` for optimal format negotiation

**Destination list (all 70 IDs and names for reference during curation):**

HIGH COUNTRY: harrietville-ovens-east (Harrietville / Ovens River East Branch), lake-eildon-fraser (Lake Eildon / Fraser Camping Area), jamieson-river-kevington (Jamieson River / Kevington), howqua-river (Howqua River / Sheepyard Flat), buckland-valley (Buckland Valley), lake-catani-buffalo (Lake Catani / Mt Buffalo), lake-william-hovell (Lake William Hovell), wonnangatta-valley (Wonnangatta Valley), king-river-valley (King River Valley), delatite-river-mansfield (Delatite River / Mansfield)

GIPPSLAND: lake-nillahcootie (Lake Nillahcootie), tidal-river-prom (Tidal River / Wilsons Promontory), thompson-river-walhalla (Thomson River / Walhalla), aberfeldy-river (Aberfeldy River), blue-rock-lake (Blue Rock Lake), mitchellriver-den-of-nargun (Mitchell River / Den of Nargun), glenmaggie-lake (Lake Glenmaggie), ninety-mile-beach-seaspray (Ninety Mile Beach / Seaspray)

GRAMPIANS/GOLDFIELDS: grampians-smiths-mill (Grampians / Smiths Mill), lake-wartook-grampians (Lake Wartook / Grampians), rocklands-reservoir (Rocklands Reservoir), cathedral-range (Cathedral Range), tullaroop-reservoir (Tullaroop Reservoir), lake-eppalock (Lake Eppalock), goulburn-river-seymour (Goulburn River / Seymour), hepburn-lagoon (Hepburn Lagoon), lauriston-reservoir (Lauriston Reservoir), cairn-curran-reservoir (Cairn Curran Reservoir), butts-reserve-maldon (Butts Reserve / Maldon)

OTWAY/WEST: aire-river-west (Aire River West), lerderderg-gorge (Lerderderg Gorge)

CENTRAL/NE RIVERS: goulburn-river-eildon-molesworth (Goulburn River / Eildon-Molesworth), waranga-basin (Waranga Basin), ovens-river-wangaratta (Ovens River / Wangaratta)

VOLCANIC PLAINS: mount-franklin (Mount Franklin), lake-purrumbete (Lake Purrumbete)

EAST GIPPSLAND: buchan-river-caves (Buchan River / Caves), dargo-river (Dargo River)

WEST GIPPSLAND: poplars-west-gippsland (The Poplars / West Gippsland)

HIGH COUNTRY CONTINUED: eildon-pondage (Eildon Pondage), nariel-creek (Nariel Creek), buffalo-river-retreat (Buffalo River Retreat), holland-creek-tatong (Holland Creek / Tatong), rocky-valley-reservoir (Rocky Valley Reservoir), mitta-mitta-river (Mitta Mitta River), upper-goulburn-woods-point (Upper Goulburn / Woods Point)

GIPPSLAND CONTINUED: blue-pool-briagolong (Blue Pool / Briagolong), ada-river (Ada River), tyers-lake (Tyers Lake), snowy-river (Snowy River), wellington-river-licola (Wellington River / Licola), gippsland-lakes-90-mile (Gippsland Lakes / 90 Mile Beach)

CENTRAL/GOLDFIELDS CONTINUED: campaspe-river-kyneton (Campaspe River / Kyneton), andersons-garden (Anderson's Garden), laanecoorie-reservoir (Laanecoorie Reservoir), loddon-river-bridgewater (Loddon River / Bridgewater), malmsbury-reservoir (Malmsbury Reservoir)

GRAMPIANS CONTINUED: lake-fyans (Lake Fyans), horseshoe-bend-wimmera (Horseshoe Bend / Wimmera River), green-hill-lake-ararat (Green Hill Lake / Ararat)

OTWAY/SURF COAST CONTINUED: stevensons-falls-otways (Stevensons Falls / Otways), lake-colac-meredith-park (Lake Colac / Meredith Park), bear-gully-walkerville (Bear Gully / Walkerville)

MURRAY RIVER: echuca-murray-river (Echuca / Murray River), gunbower-creek (Gunbower Creek)

GOULBURN/NAGAMBIE: major-creek-nagambie (Major Creek / Nagambie)

NORTHERN PLAINS: kow-swamp (Kow Swamp)

NE/HUME: lake-hume (Lake Hume)

YARRA RANGES: kurth-kiln-regional-park (Kurth Kiln Regional Park), upper-yarra-reservoir (Upper Yarra Reservoir)

Each entry's `photos` field goes after `bookingInfo`, matching the interface field order:
```typescript
  {
    id: "harrietville-ovens-east",
    // ... existing fields ...
    bookingInfo: { ... },
    photos: ["https://images.unsplash.com/photo-XXXXXXXXX?w=800&auto=format"],
  },
```

After adding all 70, run `npx tsc --noEmit` to verify there are no TypeScript errors (the non-optional field will cause a compile error if any entry is missing its `photos` array).
  </action>
  <acceptance_criteria>
    - `grep -c 'photos: \[' src/data/destinations.ts` returns exactly 70
    - `grep -c 'images.unsplash.com/photo-' src/data/destinations.ts` returns at least 70
    - Every Unsplash URL contains `?w=800&auto=format` or `?w=800&` with `auto=format` in the query string
    - `npx tsc --noEmit` exits with code 0 (no TypeScript errors)
    - No destination entry has `photos: []` (empty array)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && echo "TypeScript OK" && echo "Photo entries:" && grep -c "photos: \[" src/data/destinations.ts && echo "Unsplash URLs:" && grep -c "images.unsplash.com" src/data/destinations.ts</automated>
  </verify>
  <done>All 70 destinations have a `photos` array with one curated Unsplash CDN URL each. TypeScript compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unsplash CDN -> browser | External image URLs loaded at runtime from third-party CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | destinations.ts photo URLs | accept | URLs are static strings in source code, not user-editable. A malicious URL would only be possible via a code commit. |
| T-03-02 | Denial of Service | Unsplash CDN unavailability | mitigate | Plan 02 adds onerror fallback (D-08, D-09) so broken images degrade gracefully |
| T-03-03 | Information Disclosure | Unsplash CDN requests | accept | Unsplash URLs are public, no auth tokens embedded. Referer header sent is standard browser behavior. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `grep -c 'photos: \[' src/data/destinations.ts` returns 70
- No entry has an empty `photos` array
- All URLs follow the pattern `https://images.unsplash.com/photo-*?w=800&auto=format`
</verification>

<success_criteria>
The Destination interface includes `photos: string[]` and all 70 destination entries have a populated photos array with one Unsplash CDN URL each. TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-destination-photos/03-01-SUMMARY.md`
</output>
