---
phase: 02-critical-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Map.tsx
autonomous: true
requirements:
  - BUG-COORDS
  - BUG-CACHE

must_haves:
  truths:
    - "SpotlightCard drive estimate fallback uses the current user's homeLocation coordinates, not hardcoded central-Vic coordinates"
    - "A user with homeLocation outside Victoria sees a plausible straight-line drive estimate in SpotlightCard before their cache is built"
    - "The drive cache progress bar total matches the actual number of destinations, not the hardcoded value 70"
  artifacts:
    - path: "src/pages/Map.tsx"
      provides: "SpotlightCard with userHomeLocation prop; checkAndBuildCache with destinations.length total"
      contains: "userHomeLocation: HomeLocation | null"
  key_links:
    - from: "src/pages/Map.tsx (SpotlightCard callsite ~line 908)"
      to: "src/pages/Map.tsx (SpotlightCard component ~line 113)"
      via: "userHomeLocation={currentUser?.homeLocation ?? null} prop"
      pattern: "userHomeLocation"
    - from: "src/pages/Map.tsx (checkAndBuildCache ~line 441)"
      to: "destinations array (imported at top of file)"
      via: "destinations.length"
      pattern: "destinations\\.length"
---

<objective>
Fix two bugs in Map.tsx: incorrect hardcoded coordinates in SpotlightCard's haversine fallback, and a hardcoded progress total that desyncs when destinations change.

Purpose: BUG-COORDS causes every user outside central Victoria to see a wrong drive estimate in the spotlight row before their cache is built. BUG-CACHE means the drive cache progress bar jumps to "70" regardless of how many destinations actually exist, desyncing when destinations are added or removed.

Output:
- SpotlightCard accepts a userHomeLocation prop and uses it instead of lat1=-37.0, lng1=144.5.
- checkAndBuildCache initialises setCacheProgress total from destinations.length instead of the literal 70.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-critical-bug-fixes/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BUG-COORDS — Replace hardcoded coordinates in SpotlightCard with userHomeLocation prop</name>
  <files>src/pages/Map.tsx</files>

  <read_first>
    - src/pages/Map.tsx (lines 113–131 — SpotlightCard component definition and haversine IIFE)
    - src/pages/Map.tsx (lines 900–915 — SpotlightCard callsite in JSX)
    - src/types/index.ts (HomeLocation interface — verify field names lat and lng)
  </read_first>

  <action>
**Step A — Update SpotlightCard prop interface (lines 113–121):**

Current:
```typescript
function SpotlightCard({
  dest,
  driveCache,
  onTap,
}: {
  dest: Destination
  driveCache: DriveCache | null
  onTap: (dest: Destination) => void
}) {
```

Replace with (per D-03, D-04):
```typescript
function SpotlightCard({
  dest,
  driveCache,
  userHomeLocation,
  onTap,
}: {
  dest: Destination
  driveCache: DriveCache | null
  userHomeLocation: HomeLocation | null
  onTap: (dest: Destination) => void
}) {
```

**Step B — Replace hardcoded coordinates in the haversine IIFE (line 126):**

Current line 126:
```typescript
        const R = 6371, lat1 = -37.0, lng1 = 144.5
```

Replace with:
```typescript
        const R = 6371, lat1 = userHomeLocation?.lat ?? -37.0, lng1 = userHomeLocation?.lng ?? 144.5
```

This preserves the `-37.0, 144.5` as a last-resort fallback (per D-04: if homeLocation is null the IIFE still runs but with the original central-Vic values). The null guard pattern for the outer `cached` check (line 123) is unchanged — do not touch it.

**Step C — Update the SpotlightCard callsite in JSX (~line 908):**

Current:
```tsx
<SpotlightCard key={dest.id} dest={dest} driveCache={driveCache} onTap={handleSpotlightTap} />
```

Replace with:
```tsx
<SpotlightCard key={dest.id} dest={dest} driveCache={driveCache} userHomeLocation={currentUser?.homeLocation ?? null} onTap={handleSpotlightTap} />
```

`currentUser` is already a `UserProfile | null` state variable in MapPage. `HomeLocation` is already imported via `src/types/index.ts` — confirm the import exists at the top of Map.tsx; if it is not already imported, add `HomeLocation` to the existing types import line.
  </action>

  <verify>
    <automated>grep -n "userHomeLocation" S:/Claude/routed/src/pages/Map.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "userHomeLocation" src/pages/Map.tsx` outputs `4` (prop in destructure, prop in interface, usage in IIFE, callsite) — acceptable range is 3–5
    - src/pages/Map.tsx contains `userHomeLocation?.lat ?? -37.0`
    - src/pages/Map.tsx contains `userHomeLocation?.lng ?? 144.5`
    - src/pages/Map.tsx contains `userHomeLocation={currentUser?.homeLocation ?? null}`
    - src/pages/Map.tsx does NOT contain `lat1 = -37.0, lng1 = 144.5` as bare literals (they now appear only after `??`)
    - `npm run build` exits 0 with no TypeScript errors
  </acceptance_criteria>

  <done>SpotlightCard uses the current user's homeLocation for the haversine fallback. A user in Sydney (lat~-33.9, lng~151.2) sees a distance estimate based on their actual location, not central Victoria.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: BUG-CACHE — Replace hardcoded total:70 with destinations.length in checkAndBuildCache</name>
  <files>src/pages/Map.tsx</files>

  <read_first>
    - src/pages/Map.tsx (lines 424–456 — checkAndBuildCache function and the setCacheProgress calls)
    - src/pages/Map.tsx (lines 1–20 — top of file, confirm `destinations` is imported from src/data/destinations)
  </read_first>

  <action>
Locate the `checkAndBuildCache` function (approximately line 428–455 in Map.tsx). Within it, find the line:

```typescript
setCacheProgress({ done: 0, total: 70 })
```

Replace with (per D-06):
```typescript
setCacheProgress({ done: 0, total: destinations.length })
```

Scan the rest of `checkAndBuildCache` for any other `total: 70` occurrences and apply the same substitution to each. (The `buildDriveCache` callback already receives the live `total` from driveCache.ts, so only the initialisation call needs changing.)

The `destinations` array is imported at the top of Map.tsx from `src/data/destinations`. Confirm the import exists before applying the change — it should already be present as Map.tsx uses destination data for the spotlight row.

Do not change any other code in the function: the `buildDriveCache` call, `saveDriveCache`, `setDriveCache`, and `finally { setCacheProgress(null) }` all remain unchanged.
  </action>

  <verify>
    <automated>grep -n "total: 70" S:/Claude/routed/src/pages/Map.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "total: 70" src/pages/Map.tsx` outputs `0` (the hardcoded value is gone)
    - src/pages/Map.tsx contains `total: destinations.length` within the checkAndBuildCache function
    - `npm run build` exits 0 with no TypeScript errors
  </acceptance_criteria>

  <done>The drive cache progress bar initialises with a total derived from the actual destinations array length. Adding or removing destinations from src/data/destinations.ts automatically corrects the progress indicator without any further code changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore → client | currentUser.homeLocation is read from Firestore and used in the haversine calculation |
| Static data → runtime | destinations.length is derived from a bundled TypeScript array — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | SpotlightCard haversine (homeLocation) | accept | homeLocation is set by the authenticated user during onboarding/profile. The haversine result is display-only (an estimate label) — no financial or access-control decision depends on it. Invalid lat/lng values produce a nonsensical label but no security impact. |
| T-02-05 | Information Disclosure | SpotlightCard userHomeLocation prop | accept | homeLocation is already loaded into currentUser state and used elsewhere in Map.tsx (e.g. drive cache build). Passing it to SpotlightCard does not expose it to any new surface. |
| T-02-06 | Tampering | destinations.length in cache progress | accept | destinations is a bundled static array — not user-controlled. The value cannot be manipulated at runtime. |
</threat_model>

<verification>
After both tasks complete:
1. `npm run build` exits 0
2. `grep -c "lat1 = -37.0, lng1 = 144.5" src/pages/Map.tsx` outputs 0
3. `grep -c "total: 70" src/pages/Map.tsx` outputs 0
4. Sign in as a user with homeLocation set outside Victoria — SpotlightCard fallback labels should reflect distance from their real location
5. Trigger a cache rebuild (clear driveCache from Firestore) — progress bar should show correct total
</verification>

<success_criteria>
- SpotlightCard has userHomeLocation: HomeLocation | null prop replacing the hardcoded lat1/lng1 constants
- checkAndBuildCache uses destinations.length instead of the literal 70
- Both the prop interface and the JSX callsite are updated consistently
- TypeScript build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-critical-bug-fixes/02-02-SUMMARY.md`
</output>
